{
  "name": "iris-starter-project",
  "version": "0.1.3",
  "description": "Electron starter app for IRIS with Three.js and camera selection UI",
  "main": "electron/main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electron .\"",
    "build:renderer": "vite build",
    "build:web": "cross-env NODE_OPTIONS=--max-old-space-size=1024 vite build",
    "build": "npm run build:renderer && electron-builder --win --x64",
    "start": "electron .",
    "lint": "echo 'No linter configured'",
    "check:deps": "npx npm-check-updates"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.97.0",
    "three": "0.169.0",
    "vue": "3.5.10"
  },
  "devDependencies": {
    "@electron/notarize": "^2.3.0",
    "@types/node": "^22.7.5",
    "@types/three": "^0.180.0",
    "@vitejs/plugin-vue": "6.0.0",
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3",
    "electron": "38.3.0",
    "electron-builder": "^26.8.1",
    "npm-check-updates": "^17.1.0",
    "typescript": "^5.6.2",
    "vite": "^7.3.1",
    "wait-on": "^8.0.0"
  },
  "build": {
    "appId": "com.bath-impact-lab.iris-app",
    "productName": "IRIS App",
    "files": [
      "dist/**",
      "electron/**"
    ],
    "extraResources": [
      {
        "from": "public/assets",
        "to": "assets",
        "filter": [
          "**/*"
        ]
      }
    ],
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false
    }
  }
}
